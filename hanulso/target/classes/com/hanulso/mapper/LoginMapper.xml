<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org/DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.hanulso.mapper.LoginMapper">

	<!-- <select id = "Login"  resultType="com.hanulso.dto.MemberDTO">parameterType="com.hanulso.dto.MemberDTO"
		select count(*) from jsl_member where id = #{id} and pw =#{pw}
	</select> -->
	
	<select id = "Login" resultType="com.hanulso.dto.MemberDTO"> <!-- parameterType="com.hanulso.dto.MemberDTO" -->
		select * from member where id = #{id}
	</select> 
</mapper>